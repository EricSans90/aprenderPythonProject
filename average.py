#Funció mitjana aritmètica yoqsetioxdxd
def average() :
    count = 0
    totalval = 0
    totalval = 1
    totalval = 0

    for numval in [9,"patata",12.7,False]:
        print(type(numval))
        print("ahora numval vale " + str(numval))
        #count = count + 1
        #totalval = totalval + numval
    #print('There are',count,'numbers that have a total sum value of', totalval,'.')
    #print('The average is', totalval/count)

average ()

#Preguntar:
    #Com recupero les cadenes de després del in:
